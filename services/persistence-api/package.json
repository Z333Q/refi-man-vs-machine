{
  "name": "@refi-alpha/persistence-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The ReFi persistence API: the remote mirror behind the game's persistence port. Owns domain-object <-> PostgreSQL row mapping for progress, tips, guidance, daily tape, runs, machine versions and telemetry. Host-portable (Postgres over a connection string; container-native).",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
