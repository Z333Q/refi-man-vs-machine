/// <reference types="vite/client" />

// Injected by vite.config.ts `define` so a served page can say which commit
// built it.
declare const __BUILD_SHA__: string;
declare const __BUILD_TIME__: string;
